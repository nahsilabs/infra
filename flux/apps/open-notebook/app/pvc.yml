apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: open-notebook-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd-retain
  resources:
    requests:
      storage: 10Gi
