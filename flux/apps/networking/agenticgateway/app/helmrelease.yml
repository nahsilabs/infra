apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: agenticgateway
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: agenticgateway
    namespace: flux-system
  values:
    inferenceExtension:
      enabled: true
