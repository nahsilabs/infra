apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - secret.yml
  - release.yml
  - externalsecret.yml
configMapGenerator:
  - name: searxng-configmap
    files:
      - limiter.toml
    options:
      disableNameSuffixHash: true
