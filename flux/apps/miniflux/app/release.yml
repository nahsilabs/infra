apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: miniflux
spec:
  interval: 1h
  chart:
    spec:
      chart: app-template
      version: 5.1.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  values:
    controllers:
      main:
        containers:
          main:
            image:
              repository: miniflux/miniflux
              tag: 2.3.3@sha256:49d7b60987616387c306a8023087b31f2c9b7b21288b523026cb04058e8b6dbb
            env:
              LISTEN_ADDR: "0.0.0.0:8080"
              BASE_URL: "https://rss.nahsi.dev"
              HTTPS: "1"
              RUN_MIGRATIONS: "1"
              WORKER_POOL_SIZE: "4"
              # Allow integration POSTs to private-network targets (e.g. saving
              # entries to readeck.nahsi.dev, which resolves to an internal IP).
              INTEGRATION_ALLOW_PRIVATE_NETWORKS: "1"
              DATABASE_URL:
                valueFrom:
                  secretKeyRef:
                    name: pg-miniflux-app
                    key: uri

            envFrom:
              - secretRef:
                  name: miniflux-oidc

            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: {drop: ["ALL"]}

    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: Always
        seccompProfile: {type: RuntimeDefault}

    service:
      main:
        controller: main
        ports:
          http:
            port: 8080

    persistence:
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp

    route:
      main:
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: "Knowledge"
          gethomepage.dev/name: Miniflux
          gethomepage.dev/icon: miniflux.png
          gethomepage.dev/pod-selector: "app.kubernetes.io/name=miniflux"
        parentRefs:
          - name: private-gateway
            namespace: envoy-gateway
            sectionName: https
          - name: public-gateway
            namespace: envoy-gateway
            sectionName: https
        hostnames:
          - rss.nahsi.dev
        rules:
          - matches:
              - path:
                  type: PathPrefix
                  value: /
            backendRefs:
              - identifier: main
                port: http
                weight: 1
