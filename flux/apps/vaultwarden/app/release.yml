apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vaultwarden
spec:
  interval: 1h
  chart:
    spec:
      chart: vaultwarden
      version: "0.43.1"
      sourceRef:
        kind: HelmRepository
        name: vaultwarden
        namespace: flux-system
      interval: 1h
  values:
    image:
      registry: docker.io
      repository: vaultwarden/server
      tag: 1.37.0-alpine@sha256:88b74551f5d79fd43f897c23bd74d5295a6e1fafb5d1ec23a1da92e7a0f06767
    resourceType: StatefulSet
    domain: "https://secrets.nahsi.dev"
    logging:
      logLevel: debug
    ingress:
      enabled: false
    httpRoute:
      enabled: true
      additionalAnnotations:
        gethomepage.dev/enabled: "true"
        gethomepage.dev/group: "Apps"
        gethomepage.dev/name: Vaultwarden
        gethomepage.dev/icon: vaultwarden.png
        gethomepage.dev/pod-selector: "app.kubernetes.io/name=vaultwarden"
      parentRefs:
        - name: private-gateway
          namespace: envoy-gateway
          sectionName: https
        - name: public-gateway
          namespace: envoy-gateway
          sectionName: https
      hostnames:
        - secrets.nahsi.dev
      path: /
    adminToken:
      existingSecret: vaultwarden-secret
      existingSecretKey: token
    storage:
      data:
        name: vaultwarden-data
        class: ceph-rbd
        accessMode: ReadWriteOnce
        size: 1Gi
    resources:
      requests:
        cpu: 50m
        memory: 256Mi
    database:
      type: postgresql
      existingSecret: pg-vaultwarden-app
      existingSecretKey: uri
