apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: navidrome
spec:
  interval: 1h
  chart:
    spec:
      chart: app-template
      version: 5.1.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  values:
    controllers:
      main:
        containers:
          main:
            image:
              repository: deluan/navidrome
              tag: 0.64.0@sha256:a384948b81bd1529986c5960169e7fc4fa00f46bde6bd517971a4c36671db2af
            env:
              TZ: Europe/Athens
              ND_MUSICFOLDER: /music
              ND_DATAFOLDER: /data
              ND_CACHEFOLDER: /cache
              ND_LOGLEVEL: info
              ND_PORT: "4533"
            envFrom:
              - secretRef:
                  name: navidrome-lastfm

    defaultPodOptions:
      securityContext:
        runAsUser: 65534
        runAsGroup: 65534
        fsGroup: 65534
        fsGroupChangePolicy: OnRootMismatch

    service:
      main:
        controller: main
        ports:
          http:
            port: 4533

    persistence:
      data:
        existingClaim: navidrome-data
        globalMounts:
          - path: /data
      music:
        existingClaim: navidrome-music
        globalMounts:
          - path: /music
            readOnly: true
      cache:
        type: emptyDir
        sizeLimit: 2Gi
        globalMounts:
          - path: /cache

    route:
      main:
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: "Media"
          gethomepage.dev/name: Navidrome
          gethomepage.dev/icon: navidrome.png
          gethomepage.dev/pod-selector: "app.kubernetes.io/name=navidrome"
        parentRefs:
          - name: private-gateway
            namespace: envoy-gateway
            sectionName: https
          - name: public-gateway
            namespace: envoy-gateway
            sectionName: https
        hostnames:
          - navidrome.nahsi.dev
        rules:
          - matches:
              - path:
                  type: PathPrefix
                  value: /
            backendRefs:
              - identifier: main
                port: http
                weight: 1
