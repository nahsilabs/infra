apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: actualbudget-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd-retain
  resources:
    requests:
      storage: 10Gi
