apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: deeptutor
spec:
  interval: 1h
  chart:
    spec:
      chart: app-template
      version: 5.1.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  values:
    controllers:
      main:
        replicas: 1
        strategy: Recreate
        containers:
          main:
            image:
              repository: ghcr.io/hkuds/deeptutor
              tag: 1.6.2@sha256:25ac2696d39133430625ad6791a54f92c5c51ca75b609f66cd812cc131d7207b
            env:
              TZ: Europe/Athens
            resources:
              requests:
                cpu: 100m
                memory: 256Mi
              limits:
                memory: 2Gi
            probes:
              liveness:
                enabled: true
                type: HTTP
                path: /
                port: 8001
              readiness:
                enabled: true
                type: HTTP
                path: /
                port: 8001
              startup:
                enabled: true
                type: HTTP
                path: /
                port: 8001
                spec:
                  failureThreshold: 90
                  periodSeconds: 5

    service:
      main:
        controller: main
        ports:
          http:
            port: 3782

    persistence:
      data:
        existingClaim: deeptutor-data
        globalMounts:
          - path: /app/data

    route:
      private:
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: Knowledge
          gethomepage.dev/name: DeepTutor
          gethomepage.dev/icon: https://raw.githubusercontent.com/HKUDS/DeepTutor/ce7c2dc2dc6b7ae3063e81be29608bdba4723a25/web/public/apple-touch-icon.png
          gethomepage.dev/pod-selector: "app.kubernetes.io/name=deeptutor"
        parentRefs:
          - name: private-gateway
            namespace: envoy-gateway
            sectionName: https
        hostnames:
          - deeptutor.nahsi.dev
        rules:
          - matches:
              - path:
                  type: PathPrefix
                  value: /
            timeouts:
              request: 300s
              backendRequest: 300s
            backendRefs:
              - identifier: main
                port: http
                weight: 1
