apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: deeptutor-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd-retain
  resources:
    requests:
      storage: 20Gi
