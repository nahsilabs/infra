apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: llm-d-infra
spec:
  interval: 30m
  chart:
    spec:
      chart: llm-d-infra
      version: v1.4.0
      sourceRef:
        kind: HelmRepository
        name: llm-d-infra
        namespace: flux-system
      interval: 12h
  values:
    gateway:
      enabled: false
