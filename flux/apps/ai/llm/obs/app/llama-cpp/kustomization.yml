apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - dashboard.yml
  - scrape.yml
configMapGenerator:
  - name: llama-cpp-dashboard
    files:
      - llama-cpp.json
generatorOptions:
  disableNameSuffixHash: true
