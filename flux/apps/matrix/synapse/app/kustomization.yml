apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - release.yml
  - pvc.yml
generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: synapse-config
    files:
      - homeserver.yaml=configs/homeserver.yml
      - log.config=configs/log.config
