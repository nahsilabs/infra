apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: actual-mcp
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: actual-mcp
  ingress:
    - fromEndpoints:
        - matchLabels:
            io.kubernetes.pod.namespace: agenticgateway
            gateway.networking.k8s.io/gateway-name: agenticgateway-internal
      toPorts:
        - ports:
            - port: "3600"
              protocol: TCP
