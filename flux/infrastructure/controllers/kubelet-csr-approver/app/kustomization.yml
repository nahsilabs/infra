apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configurations:
  - kustomizeconfig.yml
resources:
  - release.yml
configMapGenerator:
  - name: kubelet-csr-approver-values
    files:
      - values.yaml=./values.yml
