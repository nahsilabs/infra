apiVersion: v1
kind: Service
metadata:
  name: intel-gpu-exporter
  labels:
    app.kubernetes.io/name: intel-gpu-exporter
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: intel-gpu-exporter
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics
