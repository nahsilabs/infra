apiVersion: operator.victoriametrics.com/v1
kind: VLSingle
metadata:
  name: stack
spec:
  retentionPeriod: "30d"
  storage:
    accessModes: [ReadWriteOnce]
    resources:
      requests:
        storage: 20Gi
    storageClassName: ceph-rbd-retain
