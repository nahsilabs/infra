apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: xberg
  namespace: flux-system
spec:
  interval: 30m
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    tag: 1.0.14
  url: oci://ghcr.io/xberg-io/charts/xberg
