apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: valkey-hyperspike-operator
  namespace: flux-system
spec:
  interval: 10m
  url: oci://ghcr.io/hyperspike/valkey-operator
  layerSelector:
    mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
    operation: copy
  ref:
    tag: v0.0.61-chart
