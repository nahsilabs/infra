{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":disableRateLimiting"
  ],
  "dependencyDashboard": true,
  "flux": {
    "managerFilePatterns": ["/flux/.+\\.ya?ml$/"]
  },
  "helm-values": {
    "managerFilePatterns": ["/flux/.+\\.ya?ml$/"]
  },
  "customManagers": [
    {
      "description": "CloudNativePG Cluster imageName",
      "customType": "regex",
      "managerFilePatterns": ["/flux/.+\\.ya?ml$/"],
      "matchStrings": [
        "imageName:\\s+(?<depName>[^:\\s]+):(?<currentValue>[^@\\s]+)(?:@(?<currentDigest>sha256:[a-f0-9]+))?"
      ],
      "datasourceTemplate": "docker"
    },
    {
      "description": "Kustomization github ?ref= URLs",
      "customType": "regex",
      "managerFilePatterns": ["/flux/.+/kustomization\\.ya?ml$/"],
      "matchStrings": [
        "https://github\\.com/(?<depName>[^/]+/[^/]+)(?:/[^?\\s]*)?\\?ref=(?<currentValue>\\S+)"
      ],
      "datasourceTemplate": "github-tags"
    },
    {
      "description": "Grafana dashboards (grafana.com revision)",
      "customType": "regex",
      "managerFilePatterns": ["/flux/.+\\.ya?ml$/"],
      "matchStrings": [
        "#\\s+renovate:\\s+depName=\"(?<depName>.*)\"\\s*\\n\\s+id:\\s+(?<packageName>\\d+)\\s*\\n\\s+revision:\\s+(?<currentValue>\\d+)"
      ],
      "versioningTemplate": "regex:^(?<major>\\d+)$",
      "datasourceTemplate": "custom.grafana-dashboards"
    },
    {
      "description": "Grafana plugins (grafana.com version)",
      "customType": "regex",
      "managerFilePatterns": ["/flux/.+\\.ya?ml$/"],
      "matchStrings": [
        "#\\s+renovate:\\s+depName=\"(?<depName>[^\"]+)\"\\s*\\n\\s+-\\s+name:\\s+\\S+\\s*\\n\\s+version:\\s+(?<currentValue>\\S+)"
      ],
      "datasourceTemplate": "custom.grafana-plugins"
    }
  ],
  "customDatasources": {
    "grafana-dashboards": {
      "defaultRegistryUrlTemplate": "https://grafana.com/api/dashboards/{{packageName}}",
      "format": "json",
      "transformTemplates": [
        "{\"releases\":[{\"version\": $string(revision)}]}"
      ]
    },
    "grafana-plugins": {
      "defaultRegistryUrlTemplate": "https://grafana.com/api/plugins/{{packageName}}",
      "format": "json",
      "transformTemplates": [
        "{\"releases\":[{\"version\": $string(version)}]}"
      ]
    }
  },
  "packageRules": [
    {
      "description": "Automerge patch updates",
      "matchUpdateTypes": ["patch", "digest"],
      "automerge": true,
      "ignoreTests": true
    },
    {
      "description": "YouTube stack: automerge everything, no delay",
      "matchFileNames": ["flux/apps/youtube/**"],
      "automerge": true,
      "ignoreTests": true,
      "minimumReleaseAge": "0 seconds",
      "prPriority": 10
    }
  ]
}
